# TO-DO

* check if smooth is produced

> I think, it won't do it if Arjun is used.

* Arjun / restarts (remove node afterward, should do it)
* tests
* compilation (separate library - and separate bindings)
